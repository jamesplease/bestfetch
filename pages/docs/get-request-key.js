export default () => {
  return (
    <div className="page">
      <h1>getRequestKey()</h1>
      <p>
        A function that implements the default algorithm of generating a request
        key.
      </p>
    </div>
  );
};
