export default function bestfetch() {
  return (
    <div className="page">
      <h1>bestfetch()</h1>
      <p>
        Initiates an HTTP request. It returns a{' '}
        <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise">
          Promise
        </a>{' '}
        that resolves once the response is available.
      </p>
    </div>
  );
}
